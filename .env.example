# Copy to .env and adjust for local development.
# docker-compose and backend (application-local.yml) read these.

# PostgreSQL (used by docker-compose and backend)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=realtime_collab
DB_USER=collab
DB_PASSWORD=collab_secret

# Redis (used by docker-compose and backend)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT / Auth (Phase 2+; placeholder for now)
# JWT_SECRET=your-256-bit-secret-here
# JWT_ISSUER=realtime-collab
# JWT_EXPIRY_SECONDS=86400
